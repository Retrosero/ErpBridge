@* Placeholder while the tab's session is restored. *@
<div class="page-loading" aria-busy="true" aria-live="polite">
    <MudSkeleton Width="220px" Height="36px" />
    <MudSkeleton Width="160px" Height="18px" Class="mb-4" />
    <div class="stat-grid">
        @for (var i = 0; i < 4; i++)
        {
            <MudSkeleton SkeletonType="SkeletonType.Rectangle" Height="112px" Class="rounded-lg" />
        }
    </div>
    <span class="sr-only">Yükleniyor…</span>
</div>
