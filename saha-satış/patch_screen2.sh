sed -i 's/modifier = Modifier.fillMaxWidth().padding(vertical = 8.dp)/modifier = Modifier.fillMaxWidth().padding(vertical = 8.dp)\n                                            )\n                                            if (currentSyncStats.isNotEmpty()) {\n                                                Text(\n                                                    text = currentSyncStats,\n                                                    style = MaterialTheme.typography.bodySmall,\n                                                    color = MaterialTheme.colorScheme.onSurfaceVariant\n                                                )/' app/src/main/java/com/example/ui/screens/ErpIntegrationScreen.kt
